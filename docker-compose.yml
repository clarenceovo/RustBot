version: '3.8'

services:
  rustbot:
    build:
      context: .
      dockerfile: Dockerfile
    image: rustbot:latest
    container_name: rustbot_crypto
    environment:
      - RUST_LOG=info # Example environment variable
    volumes:
      - ./data:/app/data # Example volume for persistent data
    restart: always